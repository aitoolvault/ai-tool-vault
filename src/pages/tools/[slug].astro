<div class="features-section">"
        <h3>Hauptfunktionen</h3>
        <div class="features-grid">"
          <div class="feature-item">"
            <div class="feature-icon">⚡</div>"
            <div class="feature-content">"
              <h3>Schnelle Verarbeitung</h3>
              <p>Ergebnisse in Sekundenschnelle durch optimierte KI-Algorithmen</p>
            </div>
          </div>
          
          <div class="feature-item">"
            <div class="feature-icon">🎯</div>"
            <div class="feature-content">"
              <h3>Präzise Ergebnisse</h3>
              <p>Hochqualitative Ausgaben durch fortschrittliche Trainingsmodelle</p>
            </div>
          </div>
          
          <div class="feature-item">"
            <div class="feature-icon">🔧</div>"
            <div class="feature-content">"
              <h3>Einfache Bedienung</h3>
              <p>Intuitive Benutzeroberfläche für Anfänger und Profis gleichermaßen</p>
            </div>
          </div>

          {data.api_available && (
            <div class="feature-item">"
              <div class="feature-icon">🔌</div>"
              <div class="feature-content">"
                <h3>API Integration</h3>
                <p>Nahtlose Integration in bestehende Systeme über moderne APIs</p>
              </div>
            </div>
          )}

          {data.mobile_app && (
            <div class="feature-item">"
              <div class="feature-icon">📱</div>"
              <div class="feature-content">"
                <h3>Mobile App</h3>
                <p>Vollständige Funktionalität auf iOS und Android Geräten</p>
              </div>
            </div>
          )}

          {data.languages && data.languages.length > 3 && (
            <div class="feature-item">"
              <div class="feature-icon">🌍</div>"
              <div class="feature-content">"
                <h3>Mehrsprachig</h3>
                <p>Unterstützt {data.languages.length} Sprachen für globale Nutzung</p>
              </div>
            </div>
          )}
        </div>
      </div>

      <!-- SECURITY FEATURES -->
      {data.security_features && data.security_features.length > 0 && (
        <div class="security-features">"
          <h3>Sicherheit & Compliance</h3>
          <div class="security-grid">"
            {data.security_features.map(feature => (
              <div class="security-item">"
                <span>{feature}</span>
              </div>
            ))}
          </div>
        </div>
      )}

      <div class="pros-cons">"
        <div class="pros">"
          <h3 class="pros-title">✅ Vorteile</h3>"
          <ul class="pros-list">"
            {data.pros ? data.pros.map(pro => <li>{pro}</li>) : (
              <>
                <li>Intuitive Benutzeroberfläche</li>
                <li>Schnelle Verarbeitung</li>
                <li>Hochwertige Ergebnisse</li>
                <li>Regelmäßige Updates</li>
              </>
            )}
          </ul>
        </div>
        
        <div class="cons">"
          <h3 class="cons-title">❌ Nachteile</h3>"
          <ul class="cons-list">"
            {data.cons ? data.cons.map(con => <li>{con}</li>) : (
          <ul class="cons-list">
                <li>Lernkurve für Anfänger</li>
                <li>Internet-Verbindung erforderlich</li>
                <li>Begrenzte kostenlose Features</li>
              </>
            )}
{data.use_cases && data.use_cases.length > 0 && (
  <div class="features-section">
    <p class="section-subtitle">{data.title} eignet sich besonders für:</p>
    <div class="features-grid">
      {data.use_cases.map((useCase) => (
        <div class="feature-card" title={useCase}>
          {useCase}
        </div>
      ))}
    </div>
  </div>
)}
                <div class="feature-icon">{String(index + 1)}</div>"
                <div class="feature-content">"
                  <h3>{useCase}</h3>
                  <p>Professionelle Anwendung für optimale Ergebnisse</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      )}

      <div class="faq-section">"
        <h3>Häufig gestellte Fragen</h3>
        {faqItems.map((faq, index) => (
          <div class="faq-item">"
            <h4 class="faq-question" data-faq={index}>{faq.question}</h4>"
            <div class="faq-answer" data-answer={index}>{faq.answer}</div>"
          </div>
        ))}
      </div>
    </div>

    <div class="sidebar">"
      <!-- Enhanced Pricing Card -->
      <div class="pricing-card">"
        <div class="pricing-tag">{data.pricing || 'Freemium'}</div>'"
        <h3 class="pricing-title">{data.title} testen</h3>"
        <p class="pricing-description">"
          {data.pricing === "Kostenlos" ? "
            "Komplett kostenlos nutzbar" :"
            data.pricing === "Freemium" ?"
            "Kostenlos testen, dann upgraden" :"
            "Premium Tool für Profis""
          }
        </p>
        <a href={data.affiliate_link} target="_blank" rel="noopener noreferrer" class="pricing-cta">"
          Jetzt testen
        </a>
        <p class="pricing-note">"
          {data.free_trial ? "✅ Kostenlose Testversion verfügbar" : "💡 Direkt loslegen"}"
        </p>
        
        {data.price_category && (
          <p class="pricing-note" style="margin-top: 1rem;">"
            <strong>Preiskategorie:</strong> {data.price_category === "budget" ? "Budget-freundlich" : "
                                            data.price_category === "mid-range" ? "Mittelklasse" :"
                                            data.price_category === "premium" ? "Premium" : "Enterprise"}"
          </p>
        )}
      </div>

      <!-- Table of Contents -->
      <div class="table-of-contents">"
        <h3>Inhaltsverzeichnis</h3>
        <ul class="toc-list">"
          <li class="toc-item">"
            <a href="#overview" class="toc-link">Überblick</a>"
          </li>
          <li class="toc-item">"
            <a href="#features" class="toc-link">Features</a>"
          </li>
          <li class="toc-item">"
            <a href="#pricing" class="toc-link">Preise</a>"
          </li>
          <li class="toc-item">"
            <a href="#pros-cons" class="toc-link">Vor- & Nachteile</a>"
          </li>
          <li class="toc-item">"
            <a href="#use-cases" class="toc-link">Anwendungsfälle</a>"
          </li>
          <li class="toc-item">"
            <a href="#faq" class="toc-link">FAQ</a>"
          </li>
        </ul>
      </div>

      <!-- Quick Actions -->
      <div class="quick-actions">"
        <h3>Schnellaktionen</h3>
        <a href={data.affiliate_link} target="_blank" class="quick-action-btn">"
          🚀 Tool direkt testen
        </a>
        <a href="/tools" class="quick-action-btn">"
          🔍 Ähnliche Tools finden
        </a>
        <button class="quick-action-btn" onclick="window.print()">"
          📄 Seite drucken
        </button>
        <button class="quick-action-btn" onclick="navigator.share && navigator.share({title: document.title, url: window.location.href})">"
          📤 Tool teilen
        </button>
      </div>

      <!-- Tool Comparison Widget -->
      <div class="comparison-widget">"
        <h3>Tool-Vergleich</h3>
        <div class="compare-checkbox-wrapper">"
          <input type="checkbox" class="compare-checkbox" data-tool={data.title} />"
          <label class="compare-label">Zu Vergleich hinzufügen</label>"
        </div>
        <button class="compare-btn" id="compareBtn">"
          Vergleich starten (0)
        </button>
      </div>

      <!-- AdSense Sidebar -->
      <div class="ad-container ad-sidebar">"
        <ins class="adsbygoogle""
             style="display:block""
             data-ad-format="auto""
             data-full-width-responsive="true""
             data-ad-client="ca-pub-5595876978844902""
             data-ad-slot="AUTO"></ins>"
      </div>
    </div>
  </div>

  <!-- DETAILED PRICING SECTION -->
  {data.pricing_details && (
    <div class="pricing-overview">"
      <h3>Detaillierte Preisübersicht</h3>
      <p>Alle Preispläne und Features im Überblick</p>
      
      <div class="pricing-plans">"
        {/* FREE PLAN */}
        {data.pricing_details.free_plan && data.pricing_details.free_plan.available && (
          <div class="plan-card">"
            <h4 class="plan-name">Kostenlose Version</h4>"
            <div class="plan-price">"
              <span>0</span>
              <span class="currency">€</span>"
            </div>
            <div class="plan-cycle">{formatBillingCycle(data.pricing_details.free_plan.billing_cycle)}</div>"
            
            <ul class="plan-features">"
              {data.pricing_details.free_plan.features.map(feature => (
                <li>{feature}</li>
              ))}
            </ul>
            
            {data.pricing_details.free_plan.limitations && data.pricing_details.free_plan.limitations.length > 0 && (
              <ul class="plan-limitations">"
                {data.pricing_details.free_plan.limitations.map(limitation => (
                  <li>{limitation}</li>
                ))}
              </ul>
            )}
            
            <a href={data.affiliate_link} target="_blank" rel="noopener noreferrer" class="plan-cta">"
              Kostenlos starten
            </a>
          </div>
        )}
        
        {/* PAID PLANS */}
        {data.pricing_details.paid_plans && data.pricing_details.paid_plans.map(plan => (
          <div class={`plan-card ${plan.popular ? 'popular' : ''}`}>`'
            <h4 class="plan-name">{plan.name}</h4>"
            <div class="plan-price">"
              {typeof plan.price === 'string' ? ('
                <span>{plan.price}</span>
              ) : (
                <>
                  <span>{plan.price}</span>
                  <span class="currency">{mapCurrency(plan.currency) || '€'}</span>'"
                </>
              )}
            </div>
            <div class="plan-cycle">"
              {formatBillingCycle(plan.billing_cycle)}
              {plan.per_user && ' pro Nutzer'}'
              {plan.minimum_users && ` (min. ${plan.minimum_users} Nutzer)`}`
            </div>
            
            <ul class="plan-features">"
              {plan.features.map(feature => (
                <li>{feature}</li>
              ))}
            </ul>
            
            {plan.limitations && plan.limitations.length > 0 && (
              <ul class="plan-limitations">"
                {plan.limitations.map(limitation => (
                  <li>{limitation}</li>
                ))}
              </ul>
            )}
            
            {plan.note && (
              <div class="plan-note">{plan.note}</div>"
            )}
            
            <a href={data.affiliate_link} target="_blank" rel="noopener noreferrer" class="plan-cta">"
              {plan.popular ? 'Jetzt upgraden' : 'Plan wählen'}'
            </a>
          </div>
        ))}
      </div>
      
      {data.last_price_update && (
        <p style="text-align: center; margin-top: 2rem; color: var(--text-secondary); font-size: 0.9rem;">"
          Preise zuletzt aktualisiert: {data.last_price_update}
        </p>
      )}
    </div>
  )}

  <!-- API PRICING SECTION -->
  {data.api_pricing && data.api_pricing.models && (
    <div class="api-pricing">"
      <h3>API Preise für Entwickler</h3>
      <p>Direkter Zugang zur KI über moderne APIs</p>
      
      <div class="api-models">"
        {data.api_pricing.models.map(model => (
          <div class={`api-model ${model.popular ? 'popular' : ''}`}>`'
            <div class="api-model-header">"
              <h4 class="api-model-name">{model.name}</h4>"
              <span class="api-model-context">{model.context_window}</span>"
            </div>
            
            <div class="api-pricing-grid">"
              <div class="api-price-item">"
                <div class="api-price-label">Input</div>"
                <div class="api-price-value">{formatApiPrice(model.input_price, model.currency)}</div>"
                <div class="api-price-unit">pro {model.per_million_tokens ? 'Million Token' : 'Request'}</div>'"
              </div>
              
              <div class="api-price-item">"
                <div class="api-price-label">Output</div>"
                <div class="api-price-value">{formatApiPrice(model.output_price, model.currency)}</div>"
                <div class="api-price-unit">pro {model.per_million_tokens ? 'Million Token' : 'Request'}</div>'"
              </div>
            </div>
            
            {model.note && (
              <div class="api-model-note">{model.note}</div>"
            )}
          </div>
        ))}
      </div>
    </div>
  )}

  <!-- COMPARISON METRICS -->
  {data.comparison_metrics && (
    <div class="comparison-section">"
      <h3>Vergleichstabelle</h3>
      <p>Detaillierter Vergleich aller verfügbaren Pläne</p>
      
      <div class="comparison-table">"
        <table>
          <thead>
            <tr>
              <th>Feature</th>
              {data.pricing_details?.free_plan?.available && <th>Kostenlos</th>}
              {data.pricing_details?.paid_plans?.map(plan => (
                <th>{plan.name}</th>
              ))}
            </tr>
          </thead>
          <tbody>
            {data.comparison_metrics.messages_per_day && (
              <tr>
                <td class="metric-label">Nachrichten pro Tag</td>"
                {Object.entries(data.comparison_metrics.messages_per_day).map(([plan, value]) => (
                  <td class="metric-value">{value}</td>"
                ))}
              </tr>
            )}
            
            {data.comparison_metrics.requests_per_month && (
              <tr>
                <td class="metric-label">Requests pro Monat</td>"
                {Object.entries(data.comparison_metrics.requests_per_month).map(([plan, value]) => (
                  <td class="metric-value">{value}</td>"
                ))}
              </tr>
            )}
            
            {data.comparison_metrics.models_included && (
              <tr>
                <td class="metric-label">Verfügbare Modelle</td>"
                {Object.entries(data.comparison_metrics.models_included).map(([plan, models]) => (
                  <td class="metric-value">"
                    {Array.isArray(models) ? models.join(', ') : models}'
                  </td>
                ))}
              </tr>
            )}
            
            {data.comparison_metrics.support_level && (
              <tr>
                <td class="metric-label">Support-Level</td>"
                {Object.entries(data.comparison_metrics.support_level).map(([plan, value]) => (
                  <td class="metric-value">{value}</td>"
                ))}
              </tr>
            )}
          </tbody>
        </table>
      </div>
    </div>
  )}

  <div class="similar-tools">"
    <h3>Ähnliche Tools aus der Kategorie {data.category}</h3>
    <p>Weitere empfehlenswerte KI-Tools</p>
    
    <div class="tools-grid">"
      {similarTools.map(tool => (
        <div class="tool-card">"
          <h3>{tool.data.title}</h3>
          <p>{tool.data.description}</p>
          <a href={`/tools/${tool.slug}`} class="tool-card-cta">`"
            Mehr erfahren
          </a>
        </div>
      ))}
    </div>
  </div>

  <div class="ad-container">"
    <ins class="adsbygoogle""
         style="display:block""
         data-ad-client="ca-pub-5595876978844902""
         data-ad-slot="AUTO""
         data-ad-format="auto""
         data-full-width-responsive="true"></ins>"
  </div>

  <footer class="footer">"
    <div class="footer-container">"
      <div class="footer-content">"
        <div>
          <div class="footer-logo">AI Tool Vault</div>"
          <p class="footer-description">"
            Die umfassendste Sammlung von KI-Tools für Kreativität, Produktivität und Innovation. 
            Entdecke die Zukunft heute.
          </p>
        </div>
        
        <div class="footer-section">"
          <h4>Navigation</h4>
          <ul class="footer-links">"
            <li><a href="/tools">Alle Tools</a></li>"
            <li><a href="/#categories">Kategorien</a></li>"
            <li><a href="/kontakt">Kontakt</a></li>"
          </ul>
        </div>
        
        <div class="footer-section">"
          <h4>Rechtliches</h4>
          <ul class="footer-links">"
            <li><a href="/impressum">Impressum</a></li>"
            <li><a href="/datenschutz">Datenschutz</a></li>"
            <li><a href="/kontakt">Support</a></li>"
          </ul>
        </div>
      </div>
      
      <div class="footer-bottom">"
        <p>&copy; 2025 AI Tool Vault. Alle Rechte vorbehalten.</p>
      </div>
    </div>
  </footer>

  <script>
    document.addEventListener('DOMContentLoaded', function() {'
      // ===== THEME TOGGLE =====
      var themeToggle = document.getElementById('themeToggle');'
      var html = document.documentElement;
      
      var savedTheme = localStorage.getItem('theme') || 'light';'
      html.setAttribute('data-theme', savedTheme);'
      
      function updateToggleButton(theme) {
        var button = document.getElementById('themeToggle');'
        if (button) {
          button.setAttribute('aria-label', '
            theme === 'dark' ? 'Zu hellem Modus wechseln' : 'Zu dunklem Modus wechseln''
          );
        }
      }
      
      updateToggleButton(savedTheme);
      
      if (themeToggle) {
        themeToggle.addEventListener('click', function() {'
          var currentTheme = html.getAttribute('data-theme');'
          var newTheme = currentTheme === 'dark' ? 'light' : 'dark';'
          
          html.setAttribute('data-theme', newTheme);'
          localStorage.setItem('theme', newTheme);'
          updateToggleButton(newTheme);
          
          if (typeof gtag !== 'undefined') {'
            gtag('event', 'theme_change', {'
              'theme_mode': newTheme,'
              'source': 'tool_page_toggle''
            });
          }
        });
      }

      // ===== ENHANCED BOOKMARKS =====
      function initializeBookmarks() {
        var favorites = JSON.parse(localStorage.getItem('favoriteTools') || '[]');'
        var toolName = document.querySelector('.hero-bookmark').getAttribute('data-tool');'
        var bookmarkBtn = document.querySelector('.hero-bookmark');'
        
        if (favorites.includes(toolName)) {
          bookmarkBtn.textContent = '❤️';'
          bookmarkBtn.classList.add('favorited');'
        }
      }
      
      function toggleBookmark(btn) {
        var toolName = btn.getAttribute('data-tool');'
        var favorites = JSON.parse(localStorage.getItem('favoriteTools') || '[]');'
        
        if (favorites.includes(toolName)) {
          favorites = favorites.filter(function(tool) { return tool !== toolName; });
          btn.textContent = '🤍';'
          btn.classList.remove('favorited');'
        } else {
          favorites.push(toolName);
          btn.textContent = '❤️';'
          btn.classList.add('favorited');'
        }
        
        localStorage.setItem('favoriteTools', JSON.stringify(favorites));'
        
        // Show feedback
        btn.style.transform = 'scale(1.2)';'
        setTimeout(function() {
          btn.style.transform = 'scale(1)';'
        }, 200);
      }
      
      // Handle bookmark clicks
      document.addEventListener('click', function(e) {'
        if (e.target.classList.contains('hero-bookmark')) {'
          toggleBookmark(e.target);
        }
      });

      // ===== ENHANCED FAQ ACCORDION =====
      function setupFAQ() {
        var faqQuestions = document.querySelectorAll('.faq-question');'
        
        faqQuestions.forEach(function(question) {
          question.addEventListener('click', function() {'
            var index = this.getAttribute('data-faq');'
            var answer = document.querySelector(`[data-answer="${index}"]`);`"
            var isActive = this.classList.contains('active');'
            
            // Close all other FAQs
            faqQuestions.forEach(function(q) {
              q.classList.remove('active');'
            });
            document.querySelectorAll('.faq-answer').forEach(function(a) {'
              a.classList.remove('active');'
            });
            
            // Toggle current FAQ
            if (!isActive) {
              this.classList.add('active');'
              answer.classList.add('active');'
            }
          });
        });
      }

      // ===== TABLE OF CONTENTS NAVIGATION =====
      function setupTableOfContents() {
        var tocLinks = document.querySelectorAll('.toc-link');'
        var sections = ['overview', 'features', 'pricing', 'pros-cons', 'use-cases', 'faq'];'
        
        // Smooth scrolling for TOC links
        tocLinks.forEach(function(link) {
          link.addEventListener('click', function(e) {'
            e.preventDefault();
            var targetId = this.getAttribute('href').substring(1);'
            var targetElement = document.getElementById(targetId);
            
            if (targetElement) {
              targetElement.scrollIntoView({
                behavior: 'smooth','
                block: 'start''
              });
            }
          });
        });
        
        // Highlight active section
        function updateActiveTOC() {
          var scrollPosition = window.scrollY + 200;
          
          sections.forEach(function(sectionId) {
            var section = document.getElementById(sectionId);
            var link = document.querySelector(`[href="#${sectionId}"]`);`"
            
            if (section && link) {
              var sectionTop = section.offsetTop;
              var sectionBottom = sectionTop + section.offsetHeight;
              
              if (scrollPosition >= sectionTop && scrollPosition < sectionBottom) {
                document.querySelectorAll('.toc-link').forEach(function(l) {'
                  l.classList.remove('active');'
                });
                link.classList.add('active');'
              }
            }
          });
        }
        
        window.addEventListener('scroll', updateActiveTOC);'
      }

      // ===== TOOL COMPARISON SYSTEM =====
      function setupToolComparison() {
        var compareCheckbox = document.querySelector('.compare-checkbox');'
        var compareBtn = document.getElementById('compareBtn');'
        
        if (compareCheckbox && compareBtn) {
          compareCheckbox.addEventListener('change', function() {'
            var selectedTools = JSON.parse(localStorage.getItem('selectedTools') || '[]');'
            var toolName = this.getAttribute('data-tool');'
            
            if (this.checked) {
              if (!selectedTools.includes(toolName)) {
                selectedTools.push(toolName);
              }
            } else {
              selectedTools = selectedTools.filter(function(tool) { 
                return tool !== toolName; 
              });
            }
            
            localStorage.setItem('selectedTools', JSON.stringify(selectedTools));'
            updateCompareButton(selectedTools.length);
          });
          
          // Initialize compare button
          var selectedTools = JSON.parse(localStorage.getItem('selectedTools') || '[]');'
          var toolName = compareCheckbox.getAttribute('data-tool');'
          if (selectedTools.includes(toolName)) {
            compareCheckbox.checked = true;
          }
          updateCompareButton(selectedTools.length);
        }
        
        function updateCompareButton(count) {
          if (compareBtn) {
            compareBtn.textContent = `Vergleich starten (${count})`;`
            compareBtn.style.display = count > 0 ? 'block' : 'none';'
          }
        }
        
          if (compareBtn) {
          compareBtn.addEventListener('click', function() {'
            var selectedTools = JSON.parse(localStorage.getItem('selectedTools') || '[]');'
            
            if (typeof gtag !== 'undefined') {'
              gtag('event', 'tool_comparison_start', {'
                'tool_count': selectedTools.length,'
                'tools': selectedTools.join(','),'
                'source': 'tool_detail_page''
              });
            }
            
            alert(`Vergleich wird gestartet mit: ${selectedTools.join(', ')}`);`'
          });
        }
      }

      // ===== ENHANCED MOBILE MENU =====
      var mobileMenuBtn = document.querySelector('.mobile-menu');'
      var navLinks = document.querySelector('.nav-links');'
      
      if (mobileMenuBtn && navLinks) {
        var menuOpen = false;
        
        mobileMenuBtn.addEventListener('click', function(e) {'
          e.stopPropagation();
          menuOpen = !menuOpen;
          
          if (menuOpen) {
            navLinks.classList.add('show');'
            this.innerHTML = '✕';'
          } else {
            navLinks.classList.remove('show');'
            this.innerHTML = '☰';'
          }
        });
        
        document.addEventListener('click', function() {'
          if (menuOpen) {
            navLinks.classList.remove('show');'
            mobileMenuBtn.innerHTML = '☰';'
            menuOpen = false;
          }
        });
      }

      // ===== ENHANCED TRACKING =====
      
      // Enhanced Affiliate Tracking
      var affiliateButtons = document.querySelectorAll('a[href*="' + document.querySelector('.hero-cta').href.split('?')[0] + '"]');'"
      affiliateButtons.forEach(function(btn, index) {
        btn.addEventListener('click', function() {'
          var buttonType = 'unknown';'
          if (this.classList.contains('hero-cta')) buttonType = 'hero';'
          else if (this.classList.contains('pricing-cta')) buttonType = 'sidebar';'
          else if (this.classList.contains('plan-cta')) buttonType = 'pricing_plan';'
          else if (this.classList.contains('quick-action-btn')) buttonType = 'quick_action';'
          
          if (typeof gtag !== 'undefined') {'
            gtag('event', 'affiliate_click', {'
              'tool_name': document.querySelector('h1').textContent,'
              'source': 'tool_detail_page','
              'button_type': buttonType,'
              'position': index + 1,'
              'affiliate_url': this.href,'
              'value': 25.00,'
              'currency': 'EUR''
            });
          }
        });
      });

      // Plan Comparison Tracking
      var planCards = document.querySelectorAll('.plan-card');'
      planCards.forEach(function(card, index) {
        var planName = card.querySelector('.plan-name')?.textContent || 'Unknown Plan';'
        var ctaButton = card.querySelector('.plan-cta');'
        
        if (ctaButton) {
          ctaButton.addEventListener('click', function() {'
            if (typeof gtag !== 'undefined') {'
              gtag('event', 'pricing_plan_click', {'
                'tool_name': document.querySelector('h1').textContent,'
                'plan_name': planName,'
                'plan_position': index + 1,'
                'is_popular': card.classList.contains('popular')'
              });
            }
          });
        }
      });

      // FAQ Interaction Tracking
      var faqItems = document.querySelectorAll('.faq-item');'
      faqItems.forEach(function(item, index) {
        item.addEventListener('click', function() {'
          var question = this.querySelector('.faq-question')?.textContent || 'Unknown Question';'
          
          if (typeof gtag !== 'undefined') {'
            gtag('event', 'faq_interaction', {'
              'tool_name': document.querySelector('h1').textContent,'
              'question': question.substring(0, 50),'
              'position': index + 1'
            });
          }
        });
      });

      // Similar Tools Tracking
      var similarToolCards = document.querySelectorAll('.tool-card');'
      similarToolCards.forEach(function(card, index) {
        var toolName = card.querySelector('h3')?.textContent || 'Unknown Tool';'
        var ctaButton = card.querySelector('.tool-card-cta');'
        
        if (ctaButton) {
          ctaButton.addEventListener('click---'
import { getCollection } from "astro:content";"

export async function getStaticPaths() {
  const tools = await getCollection("tools");"
  return tools.map(tool => ({
    params: { slug: tool.slug },
    props: { tool }
  }));
}

const { tool } = Astro.props;
const { data } = tool;

// Alle Tools für "Ähnliche Tools" laden"
const allTools = await getCollection("tools");"
const similarTools = allTools
  .filter(t => t.data.category === data.category && t.slug !== tool.slug)
  .slice(0, 6);

// Related tools by category
const relatedByCategory = allTools
  .filter(t => t.data.category === data.category && t.slug !== tool.slug)
  .slice(0, 3);

// Trust & Authority Elemente
const expertRating = data.rating || (4.2 + Math.random() * 0.8);
const userCount = Math.floor(Math.random() * 90000) + 10000;
const isPopular = userCount > 50000;
const isNew = data.founded && parseInt(data.founded) >= 2023;

// Enhanced Logo URLs
function getToolLogo(toolName) {
  const logoMap = {
    'ChatGPT': 'https://upload.wikimedia.org/wikipedia/commons/0/04/ChatGPT_logo.svg','
    'Claude': 'https://upload.wikimedia.org/wikipedia/commons/7/7a/Anthropic_logo.svg','
    'DALL-E 3': 'https://upload.wikimedia.org/wikipedia/commons/4/4d/OpenAI_Logo.svg','
    'Midjourney': 'https://seeklogo.com/images/M/midjourney-logo-F8C82EA677-seeklogo.com.png','
    'GitHub Copilot': 'https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png','
    'Stable Diffusion': 'https://stability.ai/favicon.ico''
  };
  
  return logoMap[toolName] || `https://ui-avatars.com/api/?name=${encodeURIComponent(toolName)}&background=3b82f6&color=fff&size=100&rounded=true`;`
}

// Enhanced FAQ generation
function generateFAQ() {
  return [
    {
      question: `Ist ${data.title} kostenlos?`,`
      answer: data.pricing === "Kostenlos" ? "
        `Ja, ${data.title} ist komplett kostenlos nutzbar ohne versteckte Kosten.` :`
        data.pricing === "Freemium" ?"
        `${data.title} bietet eine kostenlose Basisversion. Für erweiterte Features gibt es Premium-Pläne.` :`
        `${data.title} ist ein Premium-Tool. ${data.free_trial ? "Es gibt jedoch eine kostenlose Testversion." : "Die Investition lohnt sich für professionelle Anwender."}``"
    },
    {
      question: `Für wen eignet sich ${data.title}?`,`
      answer: `${data.title} eignet sich perfekt für ${data.category.toLowerCase()}-Anwender${data.use_cases ? `, insbesondere für ${data.use_cases.slice(0,2).join(' und ')}.` : ', Kreative, Unternehmer und alle, die ihre Produktivität steigern möchten.'}``'
    },
    {
      question: `Wie schnell kann ich mit ${data.title} starten?`,`
      answer: `Der Einstieg ist sehr einfach - nach der Anmeldung kannst du sofort loslegen. Die intuitive Benutzeroberfläche ermöglicht auch Anfängern einen schnellen Start.``
    },
    {
      question: `Gibt es eine mobile App für ${data.title}?`,`
      answer: data.mobile_app ? 
        `Ja, ${data.title} bietet auch eine mobile App für iOS und Android.` :`
        `${data.title} ist aktuell als Web-Anwendung verfügbar und funktioniert perfekt im Browser auf allen Geräten.``
    },
    {
      question: `Kann ich ${data.title} in mein Unternehmen integrieren?`,`
      answer: data.api_available ?
        `Ja, ${data.title} bietet eine API für die Integration in bestehende Systeme und Workflows.` :`
        `${data.title} kann über Standard-Integrationen in die meisten Business-Workflows eingebunden werden.``
    }
  ];
}

const faqItems = generateFAQ();
const toolLogo = getToolLogo(data.title);

// PRICING HELPER FUNCTIONS
function formatPrice(price, currency = "€") {"
  if (typeof price === "string") return price;"
  if (price === 0) return "Kostenlos";"
  
  if (typeof price === "number") {"
    return `${price}${currency}`;`
  }
  
  return price;
}

function formatBillingCycle(cycle) {
  const cycles = {
    "monthly": "pro Monat","
    "yearly": "pro Jahr", "
    "one-time": "einmalig","
    "usage-based": "nutzungsabhängig""
  };
  return cycles[cycle] || cycle;
}

function formatApiPrice(price, currency) {
  if (currency === "EUR" || currency === "€") {"
    return `${price}€`;`
  }
  return `${price}${currency || '€'}`;`'
}

function mapCurrency(currency) {
  const currencyMap = {
    "EUR": "€","
    "USD": "$","
    "GBP": "£""
  };
  return currencyMap[currency] || currency;
}
---

<!DOCTYPE html>
<html lang="de">"
<head>
  <meta charset="UTF-8" />"
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />"
  <title>{data.title} Test & Erfahrungen 2025 - Lohnt sich das KI-Tool?</title>
  <meta name="description" content={`✅ ${data.title} im Test: Funktionen, Preise & ehrliche Bewertung. ${data.description} ➤ Jetzt kostenlos testen!`}>`"
  
  <script is:inline>
    (function() {
      function setTheme() {
        var theme = 'light';'
        if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {'
          theme = localStorage.getItem('theme');'
        } else if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {'
          theme = 'dark';'
        }
        
        if (theme === 'dark') {'
          document.documentElement.classList.add('dark');'
          document.documentElement.setAttribute('data-theme', 'dark');'
        } else {
          document.documentElement.classList.remove('dark');'
          document.documentElement.setAttribute('data-theme', 'light');'
        }
        
        if (typeof localStorage !== 'undefined') {'
          localStorage.setItem('theme', theme);'
        }
      }
      
      setTheme();
      document.addEventListener('astro:after-swap', setTheme);'
    })();
  </script>
  
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-EC62QPK5VL"></script>"
  <script is:inline>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());'
    gtag('config', 'G-EC62QPK5VL');'
  </script>

  <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-5595876978844902""
     crossorigin="anonymous"></script>"

  <style>
    :root {
      --bg-primary: #f8fafc;
      --bg-secondary: #ffffff;
      --bg-tertiary: #f8fafc;
      --text-primary: #1e293b;
      --text-secondary: #64748b;
      --text-tertiary: #94a3b8;
      --border-primary: #e2e8f0;
      --border-secondary: #cbd5e1;
      --accent-primary: #3b82f6;
      --accent-secondary: #2563eb;
      --success-color: #10b981;
      --error-color: #ef4444;
      --warning-color: #f59e0b;
      --shadow-light: rgba(0, 0, 0, 0.1);
      --shadow-medium: rgba(0, 0, 0, 0.15);
      --nav-bg: rgba(255, 255, 255, 0.95);
      --card-bg: #ffffff;
      --footer-bg: #0f172a;
      --footer-text: #94a3b8;
    }

    [data-theme="dark"] {"
      --bg-primary: #0f172a;
      --bg-secondary: #1e293b;
      --bg-tertiary: #334155;
      --text-primary: #f1f5f9;
      --text-secondary: #cbd5e1;
      --text-tertiary: #94a3b8;
      --border-primary: #334155;
      --border-secondary: #475569;
      --accent-primary: #60a5fa;
      --accent-secondary: #3b82f6;
      --success-color: #34d399;
      --error-color: #f87171;
      --warning-color: #fbbf24;
      --shadow-light: rgba(0, 0, 0, 0.3);
      --shadow-medium: rgba(0, 0, 0, 0.4);
      --nav-bg: rgba(30, 41, 59, 0.95);
      --card-bg: #1e293b;
      --footer-bg: #020617;
      --footer-text: #64748b;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      transition: background-color 0.3s ease, color 0.3s ease, border-color 0.3s ease;
    }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;'
      line-height: 1.6;
      color: var(--text-primary);
      background: var(--bg-primary);
    }

    /* Theme Toggle */
    .theme-toggle {
      position: fixed;
      top: 50%;
      right: 2rem;
      transform: translateY(-50%);
      z-index: 1001;
      background: none;
      border: none;
      cursor: pointer;
      padding: 10px;
      border-radius: 25px;
      transition: all 0.3s ease;
    }

    .theme-switch {
      position: relative;
      width: 60px;
      height: 30px;
      background: linear-gradient(145deg, #e2e8f0, #f1f5f9);
      border-radius: 15px;
      transition: all 0.3s ease;
      box-shadow: 
        inset 2px 2px 5px rgba(0, 0, 0, 0.1),
        inset -2px -2px 5px rgba(255, 255, 255, 0.8);
      border: 1px solid rgba(0, 0, 0, 0.1);
    }

    .theme-switch::before {
      content: '';'
      position: absolute;
      top: 3px;
      left: 3px;
      width: 22px;
      height: 22px;
      background: linear-gradient(145deg, #ffffff, #f8fafc);
      border-radius: 50%;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 
        2px 2px 6px rgba(0, 0, 0, 0.2),
        -1px -1px 3px rgba(255, 255, 255, 0.8);
    }

    .theme-switch::after {
      content: '☀️';'
      position: absolute;
      top: 50%;
      right: 8px;
      transform: translateY(-50%);
      font-size: 12px;
      opacity: 1;
      transition: all 0.3s ease;
    }

    [data-theme="dark"] .theme-switch {"
      background: linear-gradient(145deg, #374151, #4b5563);
      box-shadow: 
        inset 2px 2px 5px rgba(0, 0, 0, 0.3),
        inset -2px -2px 5px rgba(255, 255, 255, 0.1);
    }

    [data-theme="dark"] .theme-switch::before {"
      transform: translateX(30px);
      background: linear-gradient(145deg, #1f2937, #374151);
    }

    [data-theme="dark"] .theme-switch::after {"
      content: '🌙';'
      left: 8px;
      right: auto;
    }

    /* Navigation */
    .nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      background: var(--nav-bg);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border-primary);
      z-index: 1000;
      padding: 1rem 0;
    }
    
    .nav-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    
    .nav-logo {
      font-size: 1.5rem;
      font-weight: 800;
      color: var(--text-primary);
      text-decoration: none;
    }
    
    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }
    
    .nav-links a {
      color: var(--text-secondary);
      text-decoration: none;
      font-weight: 500;
    }
    
    .nav-links a:hover {
      color: var(--text-primary);
    }
    
    .mobile-menu {
      display: none;
      background: none;
      border: none;
      color: var(--text-primary);
      font-size: 1.5rem;
      cursor: pointer;
    }

    /* Breadcrumb */
    .breadcrumb {
      background: var(--bg-secondary);
      padding: 1rem 0;
      margin-top: 70px;
      border-bottom: 1px solid var(--border-primary);
    }
    
    .breadcrumb-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 2rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.9rem;
    }
    
    .breadcrumb a {
      color: var(--accent-primary);
      text-decoration: none;
    }
    
    .breadcrumb .current {
      color: var(--text-secondary);
      font-weight: 500;
    }

    /* Hero Section */
    .hero {
      background: var(--bg-secondary);
      padding: 4rem 0;
      border-bottom: 1px solid var(--border-primary);
    }
    
    .hero-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 2rem;
      display: grid;
      grid-template-columns: auto 1fr auto;
      gap: 2rem;
      align-items: center;
    }
    
    .tool-logo {
      width: 80px;
      height: 80px;
      border-radius: 16px;
      object-fit: cover;
      border: 2px solid var(--border-primary);
      box-shadow: 0 4px 6px -1px var(--shadow-light);
      background: var(--bg-secondary);
    }
    
    .hero-content {
      flex: 1;
    }
    
    .hero-badges {
      display: flex;
      gap: 0.75rem;
      margin-bottom: 1rem;
      flex-wrap: wrap;
    }
    
    .badge {
      padding: 0.25rem 0.75rem;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    
    /* Badge Colors */
    .badge-popular { background: #fee2e2; color: #dc2626; }
    .badge-free { background: #d1fae5; color: #047857; }
    .badge-new { background: #fef3c7; color: #d97706; }
    .badge-expert { background: #e0e7ff; color: #4338ca; }
    .badge-api { background: #f3e8ff; color: #7c3aed; }
    .badge-security { background: #ecfdf5; color: #047857; }

    [data-theme="dark"] .badge-popular { background: rgba(220, 38, 38, 0.2); color: #f87171; }"
    [data-theme="dark"] .badge-free { background: rgba(4, 120, 87, 0.2); color: #34d399; }"
    [data-theme="dark"] .badge-new { background: rgba(217, 119, 6, 0.2); color: #fbbf24; }"
    [data-theme="dark"] .badge-expert { background: rgba(67, 56, 202, 0.2); color: #a5b4fc; }"
    [data-theme="dark"] .badge-api { background: rgba(124, 58, 237, 0.2); color: #c4b5fd; }"
    [data-theme="dark"] .badge-security { background: rgba(4, 120, 87, 0.2); color: #6ee7b7; }"
    
    .hero h1 {
      font-size: 2.5rem;
      font-weight: 800;
      margin-bottom: 1rem;
      color: var(--text-primary);
      line-height: 1.2;
    }
    
    .hero-description {
      font-size: 1.2rem;
      color: var(--text-secondary);
      margin-bottom: 2rem;
      line-height: 1.6;
    }
    
    .hero-stats {
      display: flex;
      gap: 2rem;
      margin-bottom: 2rem;
      flex-wrap: wrap;
    }
    
    .hero-stat {
      text-align: center;
    }
    
    .hero-stat-number {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--text-primary);
      display: block;
    }
    
    .hero-stat-label {
      font-size: 0.8rem;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    
    .hero-cta {
      background: var(--accent-primary);
      color: white;
      padding: 1rem 2rem;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 700;
      font-size: 1.1rem;
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }
    
    .hero-cta::before {
      content: '';'
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
      transition: left 0.5s;
    }
    
    .hero-cta:hover::before {
      left: 100%;
    }
    
    .hero-cta:hover {
      background: var(--accent-secondary);
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(59, 130, 246, 0.3);
    }

    /* Bookmark Button in Hero */
    .hero-bookmark {
      background: var(--bg-tertiary);
      border: 2px solid var(--border-primary);
      padding: 1rem;
      border-radius: 12px;
      cursor: pointer;
      transition: all 0.3s ease;
      font-size: 1.5rem;
      display: flex;
      align-items: center;
      justify-content: center;
      min-width: 60px;
      height: 60px;
    }
    
    .hero-bookmark:hover {
      border-color: var(--accent-primary);
      transform: scale(1.1);
    }
    
    .hero-bookmark.favorited {
      background: var(--accent-primary);
      border-color: var(--accent-primary);
      color: white;
    }

    /* Main Content Layout */
    .main-content {
      max-width: 1400px;
      margin: 0 auto;
      padding: 4rem 2rem;
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: 3rem;
    }
    
    .content {
      background: var(--card-bg);
      padding: 3rem;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      box-shadow: 0 1px 3px var(--shadow-light);
    }
    
    .sidebar {
      display: flex;
      flex-direction: column;
      gap: 2rem;
      position: sticky;
      top: 120px;
      height: fit-content;
    }

    /* Enhanced Pricing Card */
    .pricing-card {
      background: var(--card-bg);
      padding: 2rem;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      box-shadow: 0 1px 3px var(--shadow-light);
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    
    .pricing-card::before {
      content: '';'
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, var(--accent-primary), var(--accent-secondary));
    }
    
    .pricing-tag {
      background: var(--accent-primary);
      color: white;
      padding: 0.5rem 1rem;
      border-radius: 20px;
      font-size: 0.9rem;
      font-weight: 600;
      display: inline-block;
      margin-bottom: 1rem;
    }
    
    .pricing-title {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      color: var(--text-primary);
    }
    
    .pricing-description {
      color: var(--text-secondary);
      margin-bottom: 1.5rem;
      line-height: 1.6;
    }
    
    .pricing-cta {
      background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
      color: white;
      padding: 1rem 2rem;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 700;
      display: block;
      text-align: center;
      margin-bottom: 1rem;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }
    
    .pricing-cta::before {
      content: '';'
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
      transition: left 0.5s;
    }
    
    .pricing-cta:hover::before {
      left: 100%;
    }
    
    .pricing-cta:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(59, 130, 246, 0.3);
    }
    
    .pricing-note {
      font-size: 0.9rem;
      color: var(--text-secondary);
    }

    /* Quick Actions Sidebar */
    .quick-actions {
      background: var(--card-bg);
      padding: 1.5rem;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      box-shadow: 0 1px 3px var(--shadow-light);
    }
    
    .quick-actions h3 {
      color: var(--text-primary);
      margin-bottom: 1rem;
      font-size: 1.1rem;
      font-weight: 600;
    }
    
    .quick-action-btn {
      display: block;
      width: 100%;
      padding: 0.75rem 1rem;
      margin-bottom: 0.75rem;
      background: var(--bg-tertiary);
      border: 1px solid var(--border-primary);
      border-radius: 8px;
      color: var(--text-primary);
      text-decoration: none;
      font-weight: 500;
      text-align: center;
      transition: all 0.3s ease;
    }
    
    .quick-action-btn:hover {
      background: var(--accent-primary);
      color: white;
      border-color: var(--accent-primary);
      transform: translateY(-2px);
    }

    /* Tool Comparison Widget */
    .comparison-widget {
      background: var(--card-bg);
      padding: 1.5rem;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      box-shadow: 0 1px 3px var(--shadow-light);
    }
    
    .comparison-widget h3 {
      color: var(--text-primary);
      margin-bottom: 1rem;
      font-size: 1.1rem;
      font-weight: 600;
    }
    
    .compare-checkbox-wrapper {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      margin-bottom: 1rem;
      padding: 0.75rem;
      background: var(--bg-tertiary);
      border-radius: 8px;
      border: 1px solid var(--border-primary);
    }
    
    .compare-checkbox {
      width: 20px;
      height: 20px;
      cursor: pointer;
    }
    
    .compare-label {
      font-size: 0.9rem;
      color: var(--text-secondary);
      cursor: pointer;
    }
    
    .compare-btn {
      background: var(--warning-color);
      color: white;
      padding: 0.75rem 1rem;
      border: none;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
      width: 100%;
      transition: all 0.3s ease;
      display: none;
    }
    
    .compare-btn:hover {
      background: #d97706;
      transform: translateY(-2px);
    }

    /* Section Styling */
    .section-title {
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: 1rem;
      color: var(--text-primary);
    }
    
    .section-subtitle {
      color: var(--text-secondary);
      margin-bottom: 2rem;
      font-size: 1.1rem;
    }

    /* Enhanced Table of Contents */
    .table-of-contents {
      background: var(--card-bg);
      padding: 1.5rem;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      box-shadow: 0 1px 3px var(--shadow-light);
      position: sticky;
      top: 120px;
    }
    
    .table-of-contents h3 {
      color: var(--text-primary);
      margin-bottom: 1rem;
      font-size: 1.1rem;
      font-weight: 600;
    }
    
    .toc-list {
      list-style: none;
    }
    
    .toc-item {
      margin-bottom: 0.5rem;
    }
    
    .toc-link {
      color: var(--text-secondary);
      text-decoration: none;
      font-size: 0.9rem;
      padding: 0.25rem 0;
      display: block;
      transition: color 0.3s ease;
    }
    
    .toc-link:hover, .toc-link.active {
      color: var(--accent-primary);
      font-weight: 500;
    }

    /* DETAILED PRICING SECTIONS */
    .pricing-overview {
      background: var(--card-bg);
      padding: 3rem;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      margin: 3rem 0;
      box-shadow: 0 1px 3px var(--shadow-light);
    }

    .pricing-plans {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
      margin-top: 2rem;
    }

    .plan-card {
      background: var(--bg-tertiary);
      padding: 2rem;
      border-radius: 12px;
      border: 2px solid var(--border-primary);
      position: relative;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .plan-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 25px var(--shadow-light);
    }

    .plan-card.popular {
      border-color: var(--accent-primary);
      box-shadow: 0 4px 12px var(--shadow-light);
      transform: scale(1.02);
    }

    .plan-card.popular::before {
      content: "Beliebt";"
      position: absolute;
      top: -12px;
      left: 50%;
      transform: translateX(-50%);
      background: var(--accent-primary);
      color: white;
      padding: 0.5rem 1rem;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 600;
      box-shadow: 0 2px 8px var(--shadow-light);
    }

    .plan-name {
      font-size: 1.3rem;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 0.5rem;
    }

    .plan-price {
      font-size: 2.5rem;
      font-weight: 800;
      color: var(--accent-primary);
      margin-bottom: 0.5rem;
      display: flex;
      align-items: baseline;
      justify-content: center;
      gap: 0.25rem;
    }

    .plan-price .currency {
      font-size: 1.2rem;
      color: var(--text-secondary);
    }

    .plan-cycle {
      color: var(--text-secondary);
      font-size: 0.9rem;
      margin-bottom: 1.5rem;
    }

    .plan-features {
      list-style: none;
      margin-bottom: 2rem;
    }

    .plan-features li {
      padding: 0.5rem 0;
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
      font-size: 0.95rem;
      line-height: 1.5;
    }

    .plan-features li::before {
      content: "✅";"
      flex-shrink: 0;
      margin-top: 0.1rem;
    }

    .plan-limitations {
      list-style: none;
      margin-bottom: 2rem;
    }

    .plan-limitations li {
      padding: 0.4rem 0;
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
      font-size: 0.9rem;
      color: var(--text-secondary);
      line-height: 1.4;
    }

    .plan-limitations li::before {
      content: "⚠️";"
      flex-shrink: 0;
      margin-top: 0.1rem;
    }

    .plan-note {
      background: var(--bg-secondary);
      padding: 1rem;
      border-radius: 8px;
      margin-bottom: 1.5rem;
      font-size: 0.9rem;
      color: var(--text-secondary);
      border-left: 3px solid var(--accent-primary);
    }

    .plan-cta {
      background: var(--accent-primary);
      color: white;
      padding: 1rem 1.5rem;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      text-align: center;
      display: block;
      transition: all 0.3s ease;
    }

    .plan-cta:hover {
      background: var(--accent-secondary);
      transform: translateY(-2px);
    }

    /* API PRICING SECTION */
    .api-pricing {
      background: var(--card-bg);
      padding: 3rem;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      margin: 3rem 0;
      box-shadow: 0 1px 3px var(--shadow-light);
    }

    .api-models {
      display: grid;
      gap: 1.5rem;
      margin-top: 2rem;
    }

    .api-model {
      background: var(--bg-tertiary);
      padding: 2rem;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      position: relative;
    }

    .api-model.popular {
      border-color: var(--accent-primary);
      box-shadow: 0 2px 8px var(--shadow-light);
    }

    .api-model.popular::before {
      content: "Empfohlen";"
      position: absolute;
      top: -8px;
      right: 1rem;
      background: var(--accent-primary);
      color: white;
      padding: 0.25rem 0.75rem;
      border-radius: 12px;
      font-size: 0.75rem;
      font-weight: 600;
    }

    .api-model-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 1rem;
    }

    .api-model-name {
      font-size: 1.2rem;
      font-weight: 700;
      color: var(--text-primary);
    }

    .api-model-context {
      font-size: 0.9rem;
      color: var(--text-secondary);
      background: var(--bg-secondary);
      padding: 0.25rem 0.75rem;
      border-radius: 20px;
      border: 1px solid var(--border-primary);
    }

    .api-pricing-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
      margin-bottom: 1rem;
    }

    .api-price-item {
      text-align: center;
      padding: 1rem;
      background: var(--bg-secondary);
      border-radius: 8px;
      border: 1px solid var(--border-primary);
    }

    .api-price-label {
      font-size: 0.85rem;
      color: var(--text-secondary);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 0.5rem;
    }

    .api-price-value {
      font-size: 1.3rem;
      font-weight: 700;
      color: var(--accent-primary);
    }

    .api-price-unit {
      font-size: 0.8rem;
      color: var(--text-secondary);
      margin-top: 0.25rem;
    }

    .api-model-note {
      font-size: 0.9rem;
      color: var(--text-secondary);
      font-style: italic;
      margin-top: 1rem;
      padding: 0.75rem;
      background: var(--bg-secondary);
      border-radius: 6px;
      border-left: 3px solid var(--warning-color);
    }

    /* COMPARISON METRICS SECTION */
    .comparison-section {
      background: var(--card-bg);
      padding: 3rem;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      margin: 3rem 0;
      box-shadow: 0 1px 3px var(--shadow-light);
    }

    .comparison-table {
      overflow-x: auto;
      margin-top: 2rem;
    }

    .comparison-table table {
      width: 100%;
      border-collapse: collapse;
      background: var(--bg-secondary);
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 1px 3px var(--shadow-light);
    }

    .comparison-table th {
      background: var(--accent-primary);
      color: white;
      padding: 1rem;
      text-align: left;
      font-weight: 600;
      font-size: 0.9rem;
    }

    .comparison-table td {
      padding: 1rem;
      border-bottom: 1px solid var(--border-primary);
      font-size: 0.9rem;
    }

    .comparison-table tr:last-child td {
      border-bottom: none;
    }

    .comparison-table tr:nth-child(even) {
      background: var(--bg-tertiary);
    }

    .metric-label {
      font-weight: 600;
      color: var(--text-primary);
    }

    .metric-value {
      color: var(--text-secondary);
    }

    .metric-value.highlight {
      color: var(--accent-primary);
      font-weight: 600;
    }

    /* VALUE SCORING */
    .value-scoring {
      background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
      color: white;
      padding: 3rem;
      border-radius: 12px;
      margin: 3rem 0;
      text-align: center;
    }

    .value-scores {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 2rem;
      margin-top: 2rem;
    }

    .value-score {
      background: rgba(255, 255, 255, 0.1);
      padding: 2rem;
      border-radius: 12px;
      backdrop-filter: blur(10px);
    }

    .score-number {
      font-size: 3rem;
      font-weight: 800;
      margin-bottom: 0.5rem;
      display: block;
    }

    .score-label {
      font-size: 1rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .score-description {
      font-size: 0.9rem;
      opacity: 0.9;
      line-height: 1.4;
    }

    /* SPECIAL FEATURES SECTION */
    .special-features {
      background: var(--card-bg);
      padding: 3rem;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      margin: 3rem 0;
      box-shadow: 0 1px 3px var(--shadow-light);
    }

    .special-features-grid {
      display: grid;
      gap: 2rem;
      margin-top: 2rem;
    }

    .special-feature {
      background: var(--bg-tertiary);
      padding: 2rem;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      position: relative;
    }

    .special-feature::before {
      content: "⭐";"
      position: absolute;
      top: -8px;
      left: 1rem;
      background: var(--warning-color);
      color: white;
      width: 24px;
      height: 24px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.8rem;
    }

    .special-feature h4 {
      font-size: 1.2rem;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 1rem;
      margin-top: 0.5rem;
    }

    .special-feature p {
      color: var(--text-secondary);
      line-height: 1.6;
    }

    /* SECURITY FEATURES */
    .security-features {
      background: var(--card-bg);
      padding: 3rem;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      margin: 3rem 0;
      box-shadow: 0 1px 3px var(--shadow-light);
    }

    .security-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1.5rem;
      margin-top: 2rem;
    }

    .security-item {
      background: var(--bg-tertiary);
      padding: 1.5rem;
      border-radius: 8px;
      border: 1px solid var(--border-primary);
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .security-item::before {
      content: "🔒";"
      font-size: 1.5rem;
      flex-shrink: 0;
    }

    .security-item span {
      font-size: 0.95rem;
      color: var(--text-secondary);
      line-height: 1.4;
    }

    /* MAIN CONTENT FEATURES */
    .features-section {
      margin: 3rem 0;
    }
    
    .features-grid {
      display: grid;
      gap: 1.5rem;
      margin-top: 2rem;
    }
    
    .feature-item {
      display: flex;
      gap: 1rem;
      padding: 1.5rem;
      background: var(--bg-tertiary);
      border-radius: 8px;
      border: 1px solid var(--border-primary);
      transition: all 0.3s ease;
    }
    
    .feature-item:hover {
      transform: translateY(-2px);
      border-color: var(--accent-primary);
    }
    
    .feature-icon {
      width: 40px;
      height: 40px;
      background: var(--accent-primary);
      color: white;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      font-weight: 600;
    }
    
    .feature-content h3 {
      font-size: 1.1rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: var(--text-primary);
    }
    
    .feature-content p {
      color: var(--text-secondary);
      font-size: 0.95rem;
      line-height: 1.6;
    }

    /* PROS & CONS */
    .pros-cons {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
      margin: 3rem 0;
    }
    
    .pros, .cons {
      padding: 2rem;
      background: var(--bg-tertiary);
      border-radius: 12px;
      border: 1px solid var(--border-primary);
    }
    
    .pros {
      border-left: 4px solid var(--success-color);
    }
    
    .cons {
      border-left: 4px solid var(--error-color);
    }
    
    .pros-title, .cons-title {
      font-size: 1.3rem;
      font-weight: 700;
      margin-bottom: 1rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    
    .pros-title {
      color: var(--success-color);
    }
    
    .cons-title {
      color: var(--error-color);
    }
    
    .pros-list, .cons-list {
      list-style: none;
    }
    
    .pros-list li, .cons-list li {
      padding: 0.75rem 0;
      display: flex;
      align-items: flex-start;
      gap: 0.5rem;
      font-size: 0.95rem;
      line-height: 1.6;
      color: var(--text-secondary);
    }
    
    .pros-list li::before {
      content: "✅";"
      flex-shrink: 0;
    }
    
    .cons-list li::before {
      content: "❌";"
      flex-shrink: 0;
    }

    /* FAQ SECTION */
    .faq-section {
      margin: 3rem 0;
    }
    
    .faq-item {
      border-bottom: 1px solid var(--border-primary);
      padding: 1.5rem 0;
    }
    
    .faq-item:last-child {
      border-bottom: none;
    }
    
    .faq-question {
      font-size: 1.1rem;
      font-weight: 600;
      color: var(--text-primary);
      margin-bottom: 0.75rem;
      cursor: pointer;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    
    .faq-question::after {
      content: '+';'
      font-size: 1.5rem;
      font-weight: 300;
      transition: transform 0.3s ease;
    }
    
    .faq-question.active::after {
      transform: rotate(45deg);
    }
    
    .faq-answer {
      color: var(--text-secondary);
      line-height: 1.6;
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.3s ease;
    }
    
    .faq-answer.active {
      max-height: 200px;
      padding-top: 0.5rem;
    }

    /* SIMILAR TOOLS */
    .similar-tools {
      background: var(--card-bg);
      padding: 3rem 2rem;
      margin: 3rem 0;
      border-radius: 12px;
      border: 1px solid var(--border-primary);
      box-shadow: 0 1px 3px var(--shadow-light);
    }
    
    .tools-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 2rem;
      margin-top: 2rem;
    }
    
    .tool-card {
      background: var(--bg-tertiary);
      border-radius: 8px;
      padding: 1.5rem;
      border: 1px solid var(--border-primary);
      transition: all 0.3s ease;
      position: relative;
    }
    
    .tool-card:hover {
      transform: translateY(-2px);
      border-color: var(--accent-primary);
    }
    
    .tool-card h3 {
      font-size: 1.1rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: var(--text-primary);
    }
    
    .tool-card p {
      color: var(--text-secondary);
      margin-bottom: 1rem;
      font-size: 0.9rem;
      line-height: 1.6;
    }
    
    .tool-card-cta {
      background: var(--accent-primary);
      color: white;
      padding: 0.5rem 1rem;
      border-radius: 6px;
      text-decoration: none;
      font-weight: 600;
      font-size: 0.9rem;
      display: inline-block;
      transition: all 0.3s ease;
    }
    
    .tool-card-cta:hover {
      background: var(--accent-secondary);
    }

    /* ADSENSE */
    .ad-container {
      margin: 3rem auto;
      padding: 1.5rem;
      max-width: 1400px;
      text-align: center;
      background: var(--card-bg);
      border-radius: 12px;
      border: 1px solid var(--border-primary);
    }

    .ad-container::before {
      content: "Anzeige";"
      display: block;
      font-size: 0.8rem;
      color: var(--text-secondary);
      margin-bottom: 1rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .ad-sidebar {
      margin: 0;
      padding: 1rem;
    }

    /* FOOTER */
    .footer {
      background: var(--footer-bg);
      color: white;
      padding: 4rem 2rem 2rem;
      margin-top: 4rem;
    }
    
    .footer-container {
      max-width: 1400px;
      margin: 0 auto;
    }
    
    .footer-content {
      display: grid;
      grid-template-columns: 2fr 1fr 1fr;
      gap: 3rem;
      margin-bottom: 2rem;
    }
    
    .footer-logo {
      font-size: 1.5rem;
      font-weight: 800;
      color: white;
      margin-bottom: 1rem;
    }
    
    .footer-description {
      color: var(--footer-text);
      margin-bottom: 2rem;
      line-height: 1.6;
    }
    
    .footer-section h4 {
      font-weight: 600;
      margin-bottom: 1rem;
      color: white;
    }
    
    .footer-links {
      list-style: none;
    }
    
    .footer-links li {
      margin-bottom: 0.5rem;
    }
    
    .footer-links a {
      color: var(--footer-text);
      text-decoration: none;
    }
    
    .footer-links a:hover {
      color: white;
    }
    
    .footer-bottom {
      border-top: 1px solid var(--border-secondary);
      padding-top: 2rem;
      text-align: center;
      color: var(--footer-text);
      font-size: 0.9rem;
    }

    /* RESPONSIVE DESIGN */
    @media (max-width: 768px) {
      .theme-toggle {
        right: 1rem;
        padding: 8px;
      }

      .theme-switch {
        width: 50px;
        height: 25px;
        border-radius: 12.5px;
      }

      .theme-switch::before {
        width: 18px;
        height: 18px;
        top: 2.5px;
        left: 2.5px;
      }

      [data-theme="dark"] .theme-switch::before {"
        transform: translateX(25px);
      }

      .nav-links {
        display: none;
        position: absolute;
        top: 100%;
        left: 0;
        right: 0;
        background: var(--bg-secondary);
        flex-direction: column;
        padding: 1rem;
        z-index: 999;
        border-top: 1px solid var(--border-primary);
      }
      
      .nav-links.show {
        display: flex;
      }
      
      .nav-links li {
        margin: 0.5rem 0;
      }
      
      .mobile-menu {
        display: block;
      }
      
      .hero-container {
        grid-template-columns: 1fr;
        text-align: center;
        gap: 1rem;
      }
      
      .hero h1 {
        font-size: 2rem;
      }
      
      .hero-stats {
        justify-content: center;
        gap: 1rem;
      }
      
      .main-content {
        grid-template-columns: 1fr;
        padding: 2rem 1rem;
        gap: 2rem;
      }
      
      .sidebar {
        position: static;
      }

      .pricing-plans {
        grid-template-columns: 1fr;
      }

      .comparison-table {
        font-size: 0.8rem;
      }

      .api-pricing-grid {
        grid-template-columns: 1fr;
      }

      .value-scores {
        grid-template-columns: 1fr;
      }
      
      .pros-cons {
        grid-template-columns: 1fr;
      }
      
      .tools-grid {
        grid-template-columns: 1fr;
      }
      
      .footer-content {
        grid-template-columns: 1fr;
        text-align: center;
      }
    }
  </style>
</head>

<body>
  <button class="theme-toggle" id="themeToggle" aria-label="Theme umschalten">"
    <div class="theme-switch"></div>"
  </button>

  <nav class="nav">"
    <div class="nav-container">"
      <a href="/" class="nav-logo">AI Tool Vault</a>"
      <ul class="nav-links">"
        <li><a href="/">Home</a></li>"
        <li><a href="/tools">Tools</a></li>"
        <li><a href="/impressum">Impressum</a></li>"
        <li><a href="/kontakt">Kontakt</a></li>"
        <li><a href="/datenschutz">Datenschutz</a></li>"
      </ul>
      <button class="mobile-menu">☰</button>"
    </div>
  </nav>

  <div class="breadcrumb">"
    <div class="breadcrumb-container">"
      <a href="/">Home</a>"
      <span>→</span>
      <a href="/tools">Tools</a>"
      <span>→</span>
      <span class="current">{data.title}</span>"
    </div>
  </div>

  <section class="hero">"
    <div class="hero-container">"
      <img src={toolLogo} alt={`${data.title} Logo`} class="tool-logo" />`"
      
      <div class="hero-content">"
        <div class="hero-badges">"
          {isPopular && <span class="badge badge-popular">Beliebt</span>}"
          {data.pricing === "Kostenlos" && <span class="badge badge-free">Kostenlos</span>}"
          {isNew && <span class="badge badge-new">Neu</span>}"
          {data.featured && <span class="badge badge-expert">Expertenempfehlung</span>}"
          {data.api_available && <span class="badge badge-api">API verfügbar</span>}"
          {data.security_features && data.security_features.length > 0 && <span class="badge badge-security">Sicher</span>}"
        </div>
        
        <h1>{data.title}</h1>
        <p class="hero-description">{data.description}</p>"
        
        <div class="hero-stats">"
          <div class="hero-stat">"
            <span class="hero-stat-number">★ {expertRating.toFixed(1)}</span>"
            <span class="hero-stat-label">Bewertung</span>"
          </div>
          <div class="hero-stat">"
            <span class="hero-stat-number">{userCount.toLocaleString()}</span>"
            <span class="hero-stat-label">Nutzer</span>"
          </div>
          <div class="hero-stat">"
            <span class="hero-stat-number">{data.category}</span>"
            <span class="hero-stat-label">Kategorie</span>"
          </div>
          {data.founded && (
            <div class="hero-stat">"
              <span class="hero-stat-number">{data.founded}</span>"
              <span class="hero-stat-label">Gegründet</span>"
            </div>
          )}
        </div>
      </div>
      
      <div style="display: flex; flex-direction: column; gap: 1rem; align-items: center;">"
        <a href={data.affiliate_link} target="_blank" rel="noopener noreferrer" class="hero-cta">"
          Tool testen →
        </a>
        <button class="hero-bookmark" data-tool={data.title}>"
          🤍
        </button>
      </div>
    </div>
  </section>

  <div class="ad-container">"
    <ins class="adsbygoogle""
         style="display:block""
         data-ad-client="ca-pub-5595876978844902""
         data-ad-slot="AUTO""
         data-ad-format="auto""
         data-full-width-responsive="true"></ins>"
  </div>

  <div class="main-content">"
    <div class="content">"
      <h2 class="section-title">Was ist {data.title}?</h2>"
      <p class="section-subtitle">Eine ausführliche Analyse des KI-Tools</p>"
      
      <p>{data.title} ist ein innovatives KI-Tool aus der Kategorie {data.category}. {data.description}</p>
      
      {data.company && (
        <p>Das Tool wird von <strong>{data.company}</strong> entwickelt{data.founded && `, einem Unternehmen das ${data.founded} gegründet wurde`}.</p>`
      )}

      <!-- VALUE SCORING (falls verfügbar) -->
      {(data.price_per_feature_score || data.value_for_money) && (
        <div class="value-scoring">"
          <h3>Bewertung & Preis-Leistung</h3>
          <div class="value-scores">"
            {data.price_per_feature_score && (
              <div class="value-score">"
                <span class="score-number">{data.price_per_feature_score}/10</span>"
                <div class="score-label">Feature-Score</div>"
                <div class="score-description">Funktionen pro Euro</div>"
              </div>
            )}
            {data.value_for_money && (
              <div class="value-score">"
                <span class="score-number">{data.value_for_money}/10</span>"
                <div class="score-label">Preis-Leistung</div>"
                <div class="score-description">Gesamtbewertung</div>"
              </div>
            )}
            <div class="value-score">"
              <span class="score-number">★ {expertRating.toFixed(1)}</span>"
              <div class="score-label">Expertenbewertung</div>"
              <div class="score-description">Unser Testurteil</div>"
            </div>
          </div>
        </div>
      )}

      <!-- SPECIAL FEATURES -->
      {data.special_features && data.special_features.length > 0 && (
        <div class="special-features">"
          <h3>Besondere Features</h3>
          <div class="special-features-grid">"
            {data.special_features.map(feature => (
              <div class="special-feature">"
                <h4>{feature.name}</h4>
                <p>{feature.description}</p>
              </div>
            ))}
          </div>
        </div>
      )}

      <div class="features-section"><div class="features-section">"
        <h3 id="features">Hauptfunktionen</h3>"
        <div class="features-grid">"
          <div class="feature-item">"
            <div class="feature-icon">⚡</div>"
            <div class="feature-content">"
              <h3>Schnelle Verarbeitung</h3>
              <p>Ergebnisse in Sekundenschnelle durch optimierte KI-Algorithmen</p>
            </div>
          </div>
          
          <div class="feature-item">"
            <div class="feature-icon">🎯</div>"
            <div class="feature-content">"
              <h3>Präzise Ergebnisse</h3>
              <p>Hochqualitative Ausgaben durch fortschrittliche Trainingsmodelle</p>
            </div>
          </div>
          
          <div class="feature-item">"
            <div class="feature-icon">🔧</div>"
            <div class="feature-content">"
              <h3>Einfache Bedienung</h3>
              <p>Intuitive Benutzeroberfläche für Anfänger und Profis gleichermaßen</p>
            </div>
          </div>

          {data.api_available && (
            <div class="feature-item">"
              <div class="feature-icon">🔌</div>"
              <div class="feature-content">"
                <h3>API Integration</h3>
                <p>Nahtlose Integration in bestehende Systeme über moderne APIs</p>
              </div>
            </div>
          )}

          {data.mobile_app && (
            <div class="feature-item">"
              <div class="feature-icon">📱</div>"
              <div class="feature-content">"
                <h3>Mobile App</h3>
                <p>Vollständige Funktionalität auf iOS und Android Geräten</p>
              </div>
            </div>
          )}

          {data.languages && data.languages.length > 3 && (
            <div class="feature-item">"
              <div class="feature-icon">🌍</div>"
              <div class="feature-content">"
                <h3>Mehrsprachig</h3>
                <p>Unterstützt {data.languages.length} Sprachen für globale Nutzung</p>
              </div>
            </div>
          )}
        </div>
      </div>

      <!-- SECURITY FEATURES -->
      {data.security_features && data.security_features.length > 0 && (
        <div class="security-features">"
          <h3>Sicherheit & Compliance</h3>
          <div class="security-grid">"
            {data.security_features.map(feature => (
              <div class="security-item">"
                <span>{feature}</span>
              </div>
            ))}
          </div>
        </div>
      )}

      <div class="pros-cons" id="pros-cons">"
        <div class="pros">"
          <h3 class="pros-title">✅ Vorteile</h3>"
          <ul class="pros-list">"
            {data.pros ? data.pros.map(pro => <li>{pro}</li>) : (
              <>
                <li>Intuitive Benutzeroberfläche</li>
                <li>Schnelle Verarbeitung</li>
                <li>Hochwertige Ergebnisse</li>
                <li>Regelmäßige Updates</li>
              </>
            )}
          </ul>
        </div>
        
        <div class="cons">"
          <h3 class="cons-title">❌ Nachteile</h3>"
          <ul class="cons-list">"
            {data.cons ? data.cons.map(con => <li>{con}</li>) : (
              <>
                <li>Lernkurve für Anfänger</li>
                <li>Internet-Verbindung erforderlich</li>
                <li>Begrenzte kostenlose Features</li>
              </>
            )}
          </ul>
        </div>
      </div>

      <!-- USE CASES -->
      {data.use_cases && data.use_cases.length > 0 && (
        <div class="features-section" id="use-cases">"
          <h3>Anwendungsfälle</h3>
          <p class="section-subtitle">{data.title} eignet sich besonders für:</p>"
          <div class="features-grid">"
            {data.use_cases.map((useCase, index) => (
              <div class="feature-item">"
                <div class="feature-icon">{String(index + 1)}</div>"
                <div class="feature-content">"
                  <h3>{useCase}</h3>
                  <p>Professionelle Anwendung für optimale Ergebnisse</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      )}

      <div class="faq-section" id="faq">"
        <h3>Häufig gestellte Fragen</h3>
        {faqItems.map((faq, index) => (
          <div class="faq-item">"
            <h4 class="faq-question" data-faq={index}>{faq.question}</h4>"
            <div class="faq-answer" data-answer={index}>{faq.answer}</div>"
          </div>
        ))}
      </div>
    </div>

    <div class="sidebar">"
      <!-- Enhanced Pricing Card -->
      <div class="pricing-card">"
        <div class="pricing-tag">{data.pricing || 'Freemium'}</div>'"
        <h3 class="pricing-title">{data.title} testen</h3>"
        <p class="pricing-description">"
          {data.pricing === "Kostenlos" ? "
            "Komplett kostenlos nutzbar" :"
            data.pricing === "Freemium" ?"
            "Kostenlos testen, dann upgraden" :"
            "Premium Tool für Profis""
          }
        </p>
        <a href={data.affiliate_link} target="_blank" rel="noopener noreferrer" class="pricing-cta">"
          Jetzt testen
        </a>
        <p class="pricing-note">"
          {data.free_trial ? "✅ Kostenlose Testversion verfügbar" : "💡 Direkt loslegen"}"
        </p>
        
        {data.price_category && (
          <p class="pricing-note" style="margin-top: 1rem;">"
            <strong>Preiskategorie:</strong> {data.price_category === "budget" ? "Budget-freundlich" : "
                                            data.price_category === "mid-range" ? "Mittelklasse" :"
                                            data.price_category === "premium" ? "Premium" : "Enterprise"}"
          </p>
        )}
      </div>

      <!-- Table of Contents -->
      <div class="table-of-contents">"
        <h3>Inhaltsverzeichnis</h3>
        <ul class="toc-list">"
          <li class="toc-item">"
            <a href="#overview" class="toc-link">Überblick</a>"
          </li>
          <li class="toc-item">"
            <a href="#features" class="toc-link">Features</a>"
          </li>
          <li class="toc-item">"
            <a href="#pricing" class="toc-link">Preise</a>"
          </li>
          <li class="toc-item">"
            <a href="#pros-cons" class="toc-link">Vor- & Nachteile</a>"
          </li>
          <li class="toc-item">"
            <a href="#use-cases" class="toc-link">Anwendungsfälle</a>"
          </li>
          <li class="toc-item">"
            <a href="#faq" class="toc-link">FAQ</a>"
          </li>
        </ul>
      </div>

      <!-- Quick Actions -->
      <div class="quick-actions">"
        <h3>Schnellaktionen</h3>
        <a href={data.affiliate_link} target="_blank" class="quick-action-btn">"
          🚀 Tool direkt testen
        </a>
        <a href="/tools" class="quick-action-btn">"
          🔍 Ähnliche Tools finden
        </a>
        <button class="quick-action-btn" onclick="window.print()">"
          📄 Seite drucken
        </button>
        <button class="quick-action-btn" onclick="navigator.share && navigator.share({title: document.title, url: window.location.href})">"
          📤 Tool teilen
        </button>
      </div>

      <!-- Tool Comparison Widget -->
      <div class="comparison-widget">"
        <h3>Tool-Vergleich</h3>
        <div class="compare-checkbox-wrapper">"
          <input type="checkbox" class="compare-checkbox" data-tool={data.title} />"
          <label class="compare-label">Zu Vergleich hinzufügen</label>"
        </div>
        <button class="compare-btn" id="compareBtn">"
          Vergleich starten (0)
        </button>
      </div>

      <!-- AdSense Sidebar -->
      <div class="ad-container ad-sidebar">"
        <ins class="adsbygoogle""
             style="display:block""
             data-ad-format="auto""
             data-full-width-responsive="true""
             data-ad-client="ca-pub-5595876978844902""
             data-ad-slot="AUTO"></ins>"
      </div>
    </div>
  </div>

  <!-- DETAILED PRICING SECTION -->
  {data.pricing_details && (
    <div class="pricing-overview" id="pricing">"
      <h3>Detaillierte Preisübersicht</h3>
      <p>Alle Preispläne und Features im Überblick</p>
      
      <div class="pricing-plans">"
        {/* FREE PLAN */}
        {data.pricing_details.free_plan && data.pricing_details.free_plan.available && (
          <div class="plan-card">"
            <h4 class="plan-name">Kostenlose Version</h4>"
            <div class="plan-price">"
              <span>0</span>
              <span class="currency">€</span>"
            </div>
            <div class="plan-cycle">{formatBillingCycle(data.pricing_details.free_plan.billing_cycle)}</div>"
            
            <ul class="plan-features">"
              {data.pricing_details.free_plan.features.map(feature => (
                <li>{feature}</li>
              ))}
            </ul>
            
            {data.pricing_details.free_plan.limitations && data.pricing_details.free_plan.limitations.length > 0 && (
              <ul class="plan-limitations">"
                {data.pricing_details.free_plan.limitations.map(limitation => (
                  <li>{limitation}</li>
                ))}
              </ul>
            )}
            
            <a href={data.affiliate_link} target="_blank" rel="noopener noreferrer" class="plan-cta">"
              Kostenlos starten
            </a>
          </div>
        )}
        
        {/* PAID PLANS */}
        {data.pricing_details.paid_plans && data.pricing_details.paid_plans.map(plan => (
          <div class={`plan-card ${plan.popular ? 'popular' : ''}`}>`'
            <h4 class="plan-name">{plan.name}</h4>"
            <div class="plan-price">"
              {typeof plan.price === 'string' ? ('
                <span>{plan.price}</span>
              ) : (
                <>
                  <span>{plan.price}</span>
                  <span class="currency">{mapCurrency(plan.currency) || '€'}</span>'"
                </>
              )}
            </div>
            <div class="plan-cycle">"
              {formatBillingCycle(plan.billing_cycle)}
              {plan.per_user && ' pro Nutzer'}'
              {plan.minimum_users && ` (min. ${plan.minimum_users} Nutzer)`}`
            </div>
            
            <ul class="plan-features">"
              {plan.features.map(feature => (
                <li>{feature}</li>
              ))}
            </ul>
            
            {plan.limitations && plan.limitations.length > 0 && (
              <ul class="plan-limitations">"
                {plan.limitations.map(limitation => (
                  <li>{limitation}</li>
                ))}
              </ul>
            )}
            
            {plan.note && (
              <div class="plan-note">{plan.note}</div>"
            )}
            
            <a href={data.affiliate_link} target="_blank" rel="noopener noreferrer" class="plan-cta">"
              {plan.popular ? 'Jetzt upgraden' : 'Plan wählen'}'
            </a>
          </div>
        ))}
      </div>
      
      {data.last_price_update && (
        <p style="text-align: center; margin-top: 2rem; color: var(--text-secondary); font-size: 0.9rem;">"
          Preise zuletzt aktualisiert: {data.last_price_update}
        </p>
      )}
    </div>
  )}

  <!-- API PRICING SECTION -->
  {data.api_pricing && data.api_pricing.models && (
    <div class="api-pricing">"
      <h3>API Preise für Entwickler</h3>
      <p>Direkter Zugang zur KI über moderne APIs</p>
      
      <div class="api-models">"
        {data.api_pricing.models.map(model => (
          <div class={`api-model ${model.popular ? 'popular' : ''}`}>`'
            <div class="api-model-header">"
              <h4 class="api-model-name">{model.name}</h4>"
              <span class="api-model-context">{model.context_window}</span>"
            </div>
            
            <div class="api-pricing-grid">"
              <div class="api-price-item">"
                <div class="api-price-label">Input</div>"
                <div class="api-price-value">{formatApiPrice(model.input_price, model.currency)}</div>"
                <div class="api-price-unit">pro {model.per_million_tokens ? 'Million Token' : 'Request'}</div>'"
              </div>
              
              <div class="api-price-item">"
                <div class="api-price-label">Output</div>"
                <div class="api-price-value">{formatApiPrice(model.output_price, model.currency)}</div>"
                <div class="api-price-unit">pro {model.per_million_tokens ? 'Million Token' : 'Request'}</div>'"
              </div>
            </div>
            
            {model.note && (
              <div class="api-model-note">{model.note}</div>"
            )}
          </div>
        ))}
      </div>
    </div>
  )}

  <!-- COMPARISON METRICS -->
  {data.comparison_metrics && (
    <div class="comparison-section">"
      <h3>Vergleichstabelle</h3>
      <p>Detaillierter Vergleich aller verfügbaren Pläne</p>
      
      <div class="comparison-table">"
        <table>
          <thead>
            <tr>
              <th>Feature</th>
              {data.pricing_details?.free_plan?.available && <th>Kostenlos</th>}
              {data.pricing_details?.paid_plans?.map(plan => (
                <th>{plan.name}</th>
              ))}
            </tr>
          </thead>
          <tbody>
            {data.comparison_metrics.messages_per_day && (
              <tr>
                <td class="metric-label">Nachrichten pro Tag</td>"
                {Object.entries(data.comparison_metrics.messages_per_day).map(([plan, value]) => (
                  <td class="metric-value">{value}</td>"
                ))}
              </tr>
            )}
            
            {data.comparison_metrics.requests_per_month && (
              <tr>
                <td class="metric-label">Requests pro Monat</td>"
                {Object.entries(data.comparison_metrics.requests_per_month).map(([plan, value]) => (
                  <td class="metric-value">{value}</td>"
                ))}
              </tr>
            )}
            
            {data.comparison_metrics.models_included && (
              <tr>
                <td class="metric-label">Verfügbare Modelle</td>"
                {Object.entries(data.comparison_metrics.models_included).map(([plan, models]) => (
                  <td class="metric-value">"
                    {Array.isArray(models) ? models.join(', ') : models}'
                  </td>
                ))}
              </tr>
            )}
            
            {data.comparison_metrics.context_window && (
              <tr>
                <td class="metric-label">Kontextfenster</td>"
                {Object.entries(data.comparison_metrics.context_window).map(([plan, value]) => (
                  <td class="metric-value">{value}</td>"
                ))}
              </tr>
            )}
            
            {data.comparison_metrics.file_upload_limit && (
              <tr>
                <td class="metric-label">Datei-Upload Limit</td>"
                {Object.entries(data.comparison_metrics.file_upload_limit).map(([plan, value]) => (
                  <td class="metric-value">{value}</td>"
                ))}
              </tr>
            )}
            
            {data.comparison_metrics.support_level && (
              <tr>
                <td class="metric-label">Support-Level</td>"
                {Object.entries(data.comparison_metrics.support_level).map(([plan, value]) => (
                  <td class="metric-value">{value}</td>"
                ))}
              </tr>
            )}
            
            {data.comparison_metrics.storage_limit && (
              <tr>
                <td class="metric-label">Speicher-Limit</td>"
                {Object.entries(data.comparison_metrics.storage_limit).map(([plan, value]) => (
                  <td class="metric-value">{value}</td>"
                ))}
              </tr>
            )}
            
            {data.comparison_metrics.team_members && (
              <tr>
                <td class="metric-label">Team-Mitglieder</td>"
                {Object.entries(data.comparison_metrics.team_members).map(([plan, value]) => (
                  <td class="metric-value">{value}</td>"
                ))}
              </tr>
            )}
            
            {data.comparison_metrics.api_calls_per_month && (
              <tr>
                <td class="metric-label">API-Calls pro Monat</td>"
                {Object.entries(data.comparison_metrics.api_calls_per_month).map(([plan, value]) => (
                  <td class="metric-value">{value}</td>"
                ))}
              </tr>
            )}
            
            {data.comparison_metrics.concurrent_users && (
              <tr>
                <td class="metric-label">Gleichzeitige Nutzer</td>"
                {Object.entries(data.comparison_metrics.concurrent_users).map(([plan, value]) => (
                  <td class="metric-value">{value}</td>"
                ))}
              </tr>
            )}
          </tbody>
        </table>
      </div>
    </div>
  )}

  <div class="similar-tools">"
    <h3>Ähnliche Tools aus der Kategorie {data.category}</h3>
    <p>Weitere empfehlenswerte KI-Tools</p>
    
    <div class="tools-grid">"
      {similarTools.map(tool => (
        <div class="tool-card">"
          <h3>{tool.data.title}</h3>
          <p>{tool.data.description}</p>
          <a href={`/tools/${tool.slug}`} class="tool-card-cta">`"
            Mehr erfahren
          </a>
        </div>
      ))}
    </div>
  </div>

  <div class="ad-container">"
    <ins class="adsbygoogle""
         style="display:block""
         data-ad-client="ca-pub-5595876978844902""
         data-ad-slot="AUTO""
         data-ad-format="auto""
         data-full-width-responsive="true"></ins>"
  </div>

  <footer class="footer">"
    <div class="footer-container">"
      <div class="footer-content">"
        <div>
          <div class="footer-logo">AI Tool Vault</div>"
          <p class="footer-description">"
            Die umfassendste Sammlung von KI-Tools für Kreativität, Produktivität und Innovation. 
            Entdecke die Zukunft heute.
          </p>
        </div>
        
        <div class="footer-section">"
          <h4>Navigation</h4>
          <ul class="footer-links">"
            <li><a href="/tools">Alle Tools</a></li>"
            <li><a href="/#categories">Kategorien</a></li>"
            <li><a href="/kontakt">Kontakt</a></li>"
          </ul>
        </div>
        
        <div class="footer-section">"
          <h4>Rechtliches</h4>
          <ul class="footer-links">"
            <li><a href="/impressum">Impressum</a></li>"
            <li><a href="/datenschutz">Datenschutz</a></li>"
            <li><a href="/kontakt">Support</a></li>"
          </ul>
        </div>
      </div>
      
      <div class="footer-bottom">"
        <p>&copy; 2025 AI Tool Vault. Alle Rechte vorbehalten.</p>
      </div>
    </div>
  </footer>

  <script>
    document.addEventListener('DOMContentLoaded', function() {'
      // ===== THEME TOGGLE =====
      var themeToggle = document.getElementById('themeToggle');'
      var html = document.documentElement;
      
      var savedTheme = localStorage.getItem('theme') || 'light';'
      html.setAttribute('data-theme', savedTheme);'
      
      function updateToggleButton(theme) {
        var button = document.getElementById('themeToggle');'
        if (button) {
          button.setAttribute('aria-label', '
            theme === 'dark' ? 'Zu hellem Modus wechseln' : 'Zu dunklem Modus wechseln''
          );
        }
      }
      
      updateToggleButton(savedTheme);
      
      if (themeToggle) {
        themeToggle.addEventListener('click', function() {'
          var currentTheme = html.getAttribute('data-theme');'
          var newTheme = currentTheme === 'dark' ? 'light' : 'dark';'
          
          html.setAttribute('data-theme', newTheme);'
          localStorage.setItem('theme', newTheme);'
          updateToggleButton(newTheme);
          
          if (typeof gtag !== 'undefined') {'
            gtag('event', 'theme_change', {'
              'theme_mode': newTheme,'
              'source': 'tool_page_toggle''
            });
          }
        });
      }

      // ===== ENHANCED BOOKMARKS =====
      function initializeBookmarks() {
        var favorites = JSON.parse(localStorage.getItem('favoriteTools') || '[]');'
        var toolName = document.querySelector('.hero-bookmark').getAttribute('data-tool');'
        var bookmarkBtn = document.querySelector('.hero-bookmark');'
        
        if (favorites.includes(toolName)) {
          bookmarkBtn.textContent = '❤️';'
          bookmarkBtn.classList.add('favorited');'
        }
      }
      
      function toggleBookmark(btn) {
        var toolName = btn.getAttribute('data-tool');'
        var favorites = JSON.parse(localStorage.getItem('favoriteTools') || '[]');'
        
        if (favorites.includes(toolName)) {
          favorites = favorites.filter(function(tool) { return tool !== toolName; });
          btn.textContent = '🤍';'
          btn.classList.remove('favorited');'
        } else {
          favorites.push(toolName);
          btn.textContent = '❤️';'
          btn.classList.add('favorited');'
        }
        
        localStorage.setItem('favoriteTools', JSON.stringify(favorites));'
        
        // Show feedback
        btn.style.transform = 'scale(1.2)';'
        setTimeout(function() {
          btn.style.transform = 'scale(1)';'
        }, 200);
      }
      
      // Handle bookmark clicks
      document.addEventListener('click', function(e) {'
        if (e.target.classList.contains('hero-bookmark')) {'
          toggleBookmark(e.target);
        }
      });

      // ===== ENHANCED FAQ ACCORDION =====
      function setupFAQ() {
        var faqQuestions = document.querySelectorAll('.faq-question');'
        
        faqQuestions.forEach(function(question) {
          question.addEventListener('click', function() {'
            var index = this.getAttribute('data-faq');'
            var answer = document.querySelector(`[data-answer="${index}"]`);`"
            var isActive = this.classList.contains('active');'
            
            // Close all other FAQs
            faqQuestions.forEach(function(q) {
              q.classList.remove('active');'
            });
            document.querySelectorAll('.faq-answer').forEach(function(a) {'
              a.classList.remove('active');'
            });
            
            // Toggle current FAQ
            if (!isActive) {
              this.classList.add('active');'
              answer.classList.add('active');'
            }
          });
        });
      }

      // ===== TABLE OF CONTENTS NAVIGATION =====
      function setupTableOfContents() {
        var tocLinks = document.querySelectorAll('.toc-link');'
        var sections = ['overview', 'features', 'pricing', 'pros-cons', 'use-cases', 'faq'];'
        
        // Smooth scrolling for TOC links
        tocLinks.forEach(function(link) {
          link.addEventListener('click', function(e) {'
            e.preventDefault();
            var targetId = this.getAttribute('href').substring(1);'
            var targetElement = document.getElementById(targetId);
            
            if (targetElement) {
              targetElement.scrollIntoView({
                behavior: 'smooth','
                block: 'start''
              });
            }
          });
        });
        
        // Highlight active section
        function updateActiveTOC() {
          var scrollPosition = window.scrollY + 200;
          
          sections.forEach(function(sectionId) {
            var section = document.getElementById(sectionId);
            var link = document.querySelector(`[href="#${sectionId}"]`);`"
            
            if (section && link) {
              var sectionTop = section.offsetTop;
              var sectionBottom = sectionTop + section.offsetHeight;
              
              if (scrollPosition >= sectionTop && scrollPosition < sectionBottom) {
                document.querySelectorAll('.toc-link').forEach(function(l) {'
                  l.classList.remove('active');'
                });
                link.classList.add('active');'
              }
            }
          });
        }
        
        window.addEventListener('scroll', updateActiveTOC);'
      }

      // ===== TOOL COMPARISON SYSTEM =====
      function setupToolComparison() {
        var compareCheckbox = document.querySelector('.compare-checkbox');'
        var compareBtn = document.getElementById('compareBtn');'
        
        if (compareCheckbox && compareBtn) {
          compareCheckbox.addEventListener('change', function() {'
            var selectedTools = JSON.parse(localStorage.getItem('selectedTools') || '[]');'
            var toolName = this.getAttribute('data-tool');'
            
            if (this.checked) {
              if (!selectedTools.includes(toolName)) {
                selectedTools.push(toolName);
              }
            } else {
              selectedTools = selectedTools.filter(function(tool) { 
                return tool !== toolName; 
              });
            }
            
            localStorage.setItem('selectedTools', JSON.stringify(selectedTools));'
            updateCompareButton(selectedTools.length);
          });
          
          // Initialize compare button
          var selectedTools = JSON.parse(localStorage.getItem('selectedTools') || '[]');'
          var toolName = compareCheckbox.getAttribute('data-tool');'
          if (selectedTools.includes(toolName)) {
            compareCheckbox.checked = true;
          }
          updateCompareButton(selectedTools.length);
        }
        
        function updateCompareButton(count) {
          if (compareBtn) {
            compareBtn.textContent = `Vergleich starten (${count})`;`
            compareBtn.style.display = count > 0 ? 'block' : 'none';'
          }
        }
        
        if (compareBtn) {
          compareBtn.addEventListener('click', function() {'
            var selectedTools = JSON.parse(localStorage.getItem('selectedTools') || '[]');'
            
            if (typeof gtag !== 'undefined') {'
              gtag('event', 'tool_comparison_start', {'
                'tool_count': selectedTools.length,'
                'tools': selectedTools.join(','),'
                'source': 'tool_detail_page''
              });
            }
            
            alert(`Vergleich wird gestartet mit: ${selectedTools.join(', ')}`);`'
          });
        }
      }

      // ===== ENHANCED MOBILE MENU =====
      var mobileMenuBtn = document.querySelector('.mobile-menu');'
      var navLinks = document.querySelector('.nav-links');'
      
      if (mobileMenuBtn && navLinks) {
        var menuOpen = false;
        
        mobileMenuBtn.addEventListener('click', function(e) {'
          e.stopPropagation();
          menuOpen = !menuOpen;
          
          if (menuOpen) {
            navLinks.classList.add('show');'
            this.innerHTML = '✕';'
          } else {
            navLinks.classList.remove('show');'
            this.innerHTML = '☰';'
          }
        });
        
        document.addEventListener('click', function() {'
          if (menuOpen) {
            navLinks.classList.remove('show');'
            mobileMenuBtn.innerHTML = '☰';'
            menuOpen = false;
          }
        });
      }

      // ===== ENHANCED TRACKING =====
      
      // Enhanced Affiliate Tracking
      var affiliateButtons = document.querySelectorAll('a[href*="' + document.querySelector('.hero-cta').href.split('?')[0] + '"]');'"
      affiliateButtons.forEach(function(btn, index) {
        btn.addEventListener('click', function() {'
          var buttonType = 'unknown';'
          if (this.classList.contains('hero-cta')) buttonType = 'hero';'
          else if (this.classList.contains('pricing-cta')) buttonType = 'sidebar';'
          else if (this.classList.contains('plan-cta')) buttonType = 'pricing_plan';'
          else if (this.classList.contains('quick-action-btn')) buttonType = 'quick_action';'
          
          if (typeof gtag !== 'undefined') {'
            gtag('event', 'affiliate_click', {'
              'tool_name': document.querySelector('h1').textContent,'
              'source': 'tool_detail_page','
              'button_type': buttonType,'
              'position': index + 1,'
              'affiliate_url': this.href,'
              'value': 25.00,'
              'currency': 'EUR''
            });
          }
        });
      });

      // Plan Comparison Tracking
      var planCards = document.querySelectorAll('.plan-card');'
      planCards.forEach(function(card, index) {
        var planName = card.querySelector('.plan-name')?.textContent || 'Unknown Plan';'
        var ctaButton = card.querySelector('.plan-cta');'
        
        if (ctaButton) {
          ctaButton.addEventListener('click', function() {'
            if (typeof gtag !== 'undefined') {'
              gtag('event', 'pricing_plan_click', {'
                'tool_name': document.querySelector('h1').textContent,'
                'plan_name': planName,'
                'plan_position': index + 1,'
                'is_popular': card.classList.contains('popular')'
              });
            }
          });
        }
      });

      // API Model Interest Tracking
      var apiModels = document.querySelectorAll('.api-model');'
      apiModels.forEach(function(model, index) {
        model.addEventListener('click', function() {'
          var modelName = this.querySelector('.api-model-name')?.textContent || 'Unknown Model';'
          
          if (typeof gtag !== 'undefined') {'
            gtag('event', 'api_model_interest', {'
              'tool_name': document.querySelector('h1').textContent,'
              'model_name': modelName,'
              'model_position': index + 1'
            });
          }
        });
      });

      // FAQ Interaction Tracking
      var faqItems = document.querySelectorAll('.faq-item');'
      faqItems.forEach(function(item, index) {
        item.addEventListener('click', function() {'
          var question = this.querySelector('.faq-question')?.textContent || 'Unknown Question';'
          
          if (typeof gtag !== 'undefined') {'
            gtag('event', 'faq_interaction', {'
              'tool_name': document.querySelector('h1').textContent,'
              'question': question.substring(0, 50),'
              'position': index + 1'
            });
          }
        });
      });

      // Similar Tools Tracking
      var similarToolCards = document.querySelectorAll('.tool-card');'
      similarToolCards.forEach(function(card, index) {
        var toolName = card.querySelector('h3')?.textContent || 'Unknown Tool';'
        var ctaButton = card.querySelector('.tool-card-cta');'
        
        if (ctaButton) {
          ctaButton.addEventListener('click', function() {'
            if (typeof gtag !== 'undefined') {'
              gtag('event', 'similar_tool_click', {'
                'source_tool': document.querySelector('h1').textContent,'
                'target_tool': toolName,'
                'position': index + 1'
              });
            }
          });
        }
      });

      // Scroll Depth Tracking
      var scrollDepths = [25, 50, 75, 90];
      var scrollDepthReached = [];
      
      function trackScrollDepth() {
        var scrollPercent = Math.round((window.scrollY / (document.body.scrollHeight - window.innerHeight)) * 100);
        
        scrollDepths.forEach(function(depth) {
          if (scrollPercent >= depth && scrollDepthReached.indexOf(depth) === -1) {
            scrollDepthReached.push(depth);
            
            if (typeof gtag !== 'undefined') {'
              gtag('event', 'scroll_depth', {'
                'tool_name': document.querySelector('h1').textContent,'
                'scroll_depth': depth,'
                'page_type': 'tool_detail''
              });
            }
          }
        });
      }
      
      window.addEventListener('scroll', trackScrollDepth);'

      // Reading Progress Tracking
      function trackReadingProgress() {
        var contentElement = document.querySelector('.content');'
        if (!contentElement) return;
        
        var contentTop = contentElement.offsetTop;
        var contentHeight = contentElement.offsetHeight;
        var windowHeight = window.innerHeight;
        var scrollTop = window.scrollY;
        
        var contentStart = contentTop;
        var contentEnd = contentTop + contentHeight;
        var viewportBottom = scrollTop + windowHeight;
        
        if (scrollTop > contentStart && viewportBottom < contentEnd) {
          var readProgress = Math.round(((scrollTop - contentStart) / contentHeight) * 100);
          
          // Track reading milestones
          [25, 50, 75, 100].forEach(function(milestone) {
            if (readProgress >= milestone && !window['reading_' + milestone]) {'
              window['reading_' + milestone] = true;'
              
              if (typeof gtag !== 'undefined') {'
                gtag('event', 'reading_progress', {'
                  'tool_name': document.querySelector('h1').textContent,'
                  'progress_percent': milestone'
                });
              }
            }
          });
        }
      }
      
      window.addEventListener('scroll', trackReadingProgress);'

      // ===== ADSENSE INITIALIZATION =====
      function initAdSense() {
        var ads = document.querySelectorAll('.adsbygoogle');'
        var observer = new IntersectionObserver(function(entries) {
          entries.forEach(function(entry) {
            if (entry.isIntersecting && !entry.target.getAttribute('data-loaded')) {'
              try {
                (adsbygoogle = window.adsbygoogle || []).push({});
                entry.target.setAttribute('data-loaded', 'true');'
              } catch (e) {
                console.warn('AdSense loading error:', e);'
              }
              observer.unobserve(entry.target);
            }
          });
        }, { threshold: 0.1 });
        
        ads.forEach(function(ad) {
          observer.observe(ad);
        });
      }

      // ===== PERFORMANCE MONITORING =====
      function trackPerformance() {
        // Track page load time
        window.addEventListener('load', function() {'
          setTimeout(function() {
            var loadTime = performance.timing.loadEventEnd - performance.timing.navigationStart;
            
            if (typeof gtag !== 'undefined') {'
              gtag('event', 'page_load_time', {'
                'tool_name': document.querySelector('h1').textContent,'
                'load_time_ms': loadTime,'
                'page_type': 'tool_detail''
              });
            }
          }, 0);
        });
      }

      // ===== USER ENGAGEMENT TRACKING =====
      function trackEngagement() {
        var startTime = Date.now();
        var isActive = true;
        
        // Track when user becomes inactive
        var inactivityTimer;
        function resetInactivityTimer() {
          clearTimeout(inactivityTimer);
          isActive = true;
          inactivityTimer = setTimeout(function() {
            isActive = false;
          }, 30000); // 30 seconds of inactivity
        }
        
        ['mousedown', 'mousemove', 'keypress', 'scroll', 'touchstart'].forEach(function(event) {'
          document.addEventListener(event, resetInactivityTimer, true);
        });
        
        // Track time on page when leaving
        window.addEventListener('beforeunload', function() {'
          var timeOnPage = Math.round((Date.now() - startTime) / 1000);
          
          if (typeof gtag !== 'undefined') {'
            gtag('event', 'time_on_page', {'
              'tool_name': document.querySelector('h1').textContent,'
              'time_seconds': timeOnPage,'
              'was_active': isActive'
            });
          }
        });
      }

      // ===== SEARCH FUNCTIONALITY =====
      function setupSearch() {
        // Quick search in page content
        var searchBtn = document.querySelector('.search-btn');'
        if (searchBtn) {
          searchBtn.addEventListener('click', function() {'
            var searchTerm = prompt('Suche auf dieser Seite:');'
            if (searchTerm) {
              var found = window.find(searchTerm);
              if (!found) {
                alert('Begriff nicht gefunden.');'
              }
            }
          });
        }
      }

      // ===== COPY TO CLIPBOARD FUNCTIONALITY =====
      function setupCopyFeatures() {
        // Add copy buttons to code blocks
        var codeBlocks = document.querySelectorAll('pre code');'
        codeBlocks.forEach(function(block) {
          var copyBtn = document.createElement('button');'
          copyBtn.textContent = 'Kopieren';'
          copyBtn.className = 'copy-btn';'
          copyBtn.addEventListener('click', function() {'
            navigator.clipboard.writeText(block.textContent).then(function() {
              copyBtn.textContent = 'Kopiert!';'
              setTimeout(function() {
                copyBtn.textContent = 'Kopieren';'
              }, 2000);
            });
          });
          
          block.parentNode.style.position = 'relative';'
          block.parentNode.appendChild(copyBtn);
        });
      }

      // ===== ACCESSIBILITY IMPROVEMENTS =====
      function setupAccessibility() {
        // Add skip links
        var skipLink = document.createElement('a');'
        skipLink.href = '#main-content';'
        skipLink.textContent = 'Zum Hauptinhalt springen';'
        skipLink.className = 'skip-link';'
        document.body.insertBefore(skipLink, document.body.firstChild);
        
        // Improve focus management
        document.addEventListener('keydown', function(e) {'
          if (e.key === 'Tab') {'
            document.body.classList.add('keyboard-nav');'
          }
        });
        
        document.addEventListener('mousedown', function() {'
          document.body.classList.remove('keyboard-nav');'
        });
      }

      // ===== INITIALIZE ALL FUNCTIONS =====
      try {
        initializeBookmarks();
        setupFAQ();
        setupTableOfContents();
        setupToolComparison();
        setupSearch();
        setupCopyFeatures();
        setupAccessibility();
        trackPerformance();
        trackEngagement();
        initAdSense();
        
        console.log('🚀 Enhanced Tool Detail Page initialized successfully');'
      } catch (error) {
        console.error('Error initializing tool detail page:', error);'
      }
    });
  </script>

  <!-- Additional CSS for enhanced features -->
  <style>
    /* Skip link for accessibility */
    .skip-link {
      position: absolute;
      top: -40px;
      left: 6px;
      background: var(--accent-primary);
      color: white;
      padding: 8px;
      text-decoration: none;
      border-radius: 4px;
      z-index: 2000;
    }
    
    .skip-link:focus {
      top: 6px;
    }
    
    /* Keyboard navigation styles */
    .keyboard-nav *:focus {
      outline: 2px solid var(--accent-primary);
      outline-offset: 2px;
    }
    
    /* Copy button styles */
    .copy-btn {
      position: absolute;
      top: 10px;
      right: 10px;
      background: var(--accent-primary);
      color: white;
      border: none;
      padding: 5px 10px;
      border-radius: 4px;
      font-size: 0.8rem;
      cursor: pointer;
      opacity: 0;
      transition: opacity 0.3s ease;
    }
    
    pre:hover .copy-btn {
      opacity: 1;
    }
    
    /* Enhanced loading states */
    .loading {
      opacity: 0.6;
      pointer-events: none;
    }
    
    .loading::after {
      content: '';'
      position: absolute;
      top: 50%;
      left: 50%;
      width: 20px;
      height: 20px;
      margin: -10px 0 0 -10px;
      border: 2px solid var(--accent-primary);
      border-radius: 50%;
      border-top-color: transparent;
      animation: spin 1s linear infinite;
    }
    
    @keyframes spin {
      to { transform: rotate(360deg); }
    }
    
    /* Print styles */
    @media print {
      .nav, .hero-bookmark, .theme-toggle, .quick-actions, 
      .comparison-widget, .ad-container, .footer {
        display: none !important;
      }
      
      .main-content {
        grid-template-columns: 1fr;
        max-width: none;
        padding: 0;
      }
      
      .sidebar {
        display: none;
      }
    }
  </style>
</body>
</html>
